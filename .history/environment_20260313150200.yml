name: hw4
channels:
  - pytorch
  - defaults
dependencies:
  - python>=3.8
  - pytorch>=1.3.0
  - torchvision
  - cudatoolkit>=9.2    # mac OS users should comment out this (for CPU only) or build from the source
  - scikit-learn
  - numpy
  - scipy
  - pandas
  - matplotlib==3.8
  - pytest
  - pip
  - pip:
    - delayed-assert
